data_process.py
我需要在代码上续写一段功能，目标是生成一个新的数据集。
1. 因为数据文件比较大，我拿出了10条作为数据参考，位置在RAG-ACT/data/train-example.jsonl
2. 我希望在positive_passages的第二个片段和negative_passages的前两个片段，这三个片段中随机选两条，加上positive_passages的第一条，总共三条片段来模拟RAG检索到的片段，随机种子为2025
3. 我希望调用models/Llama-3.1-8B-Instruct模型采用贪心解码去生成回答，然后调用GLM大模型API去判断该回答在不在数据集中给出的answer中，只取llama回答错误的条目作为新数据集的条目
4. 新的数据集标签为query_id_new（id为新数据集的id），query_id_old，query，answers，wrong_answer，retrieve_snippets
5. llama的提示词参照prompt.py这段代码，GLM的提示词有你自己设计，务必格式化输出，便于构造数据集
6. GLM的API key用占位符代替，我将自己进行修改
7. 可以先选择10条进行测试，由我检查过之后，没问题的话，可以大批量生成

---
1. 你先通读ACT项目，了解ACT的整个项目的工作流程

2. 我希望将ACT的方法整个迁移到RAG-ACT中(除了k-means分类)，训练集和验证集换为RAG-ACT/data/new_dataset.jsonl (按合适的比例划分数据集)，测试集为RAG-ACT/data/nq-test.jsonl，主要目的是使用ACT的方法去解决“RAG系统中检索到的无关上下文片段对生成器产生影响，进而使得即使检索到正确片段也无法回答正确“的问题

3. ACT的训练是将问题+正确答案，问题+错误答案组合起来用于训练，RAG-ACT中的训练则是将问题+检索片段+正确答案，问题+检索片段+错误答案组合起来用于训练，其他的基本都按照ACT的方法进行迁移(除了k-means分类)

4. RAG-ACT的引导向量采用平均的差分向量，后根据探针分数动态调整

5. 在你工作时，ACT中不合理的代码部分需要你做适当的修改，比如：每次训练中断之后，都需要重新训练，并没有一些措施；等等问题

6. 其余基本与ACT一致，你要深入思考并动态地、灵活地进行改动，尽量全部迁移过来，我上述提到的改动如果无法实现或者有问题，请向我提出来，不要胡编乱造

7. 完成任务后，写一份说明文档，说明哪些迁移了过来，哪些与ACT不同，哪些做了改动？新代码如何使用？